# Chip Components

"Chip" refers to small units of content to display, like a tag or formatted date.

Rule of thumb for "atoms" like chips and controls: they only use built-in HTML elements in their JSX return and don't import any other project components.
